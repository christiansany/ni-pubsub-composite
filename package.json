{
  "name": "ni-pubsub-composite",
  "version": "0.0.2",
  "description": "Composite function to add the publish/subscribe pattern to an object",
  "main": "src/index.js",
  "scripts": {
    "preversion": "npm run lint && npm test",
    "version": "",
    "postversion": "git push && git push --tags",
    "lint": "eslint **/*.js",
    "test": "babel-node test/index.js | tap-spec"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/christiansany/ni-pubsub-composite"
  },
  "keywords": [
    "pubsub",
    "publish",
    "subscribe",
    "composite"
  ],
  "author": "Christian Sany <christian.sany@bluewin.ch> (http://christiansany.ch)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-tape-runner": "^2.0.1",
    "eslint": "^3.15.0",
    "eslint-plugin-no-unsafe-innerhtml": "^1.0.15",
    "mock-local-storage": "^1.0.2",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.3"
  }
}
